# spacepy-field

```
# (Don't use longer env name; see https://github.com/spacepy/spacepy/issues/836)
conda create -n sp-field python=3.12.2 -y
conda activate sp-field
pip install -e .
python demo.py
```